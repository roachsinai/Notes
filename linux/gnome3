1. Fedora23 修改主文件夹目录名为英文 
终端中输入命令:
export LANG=en_US
xdg-user-dirs-gtk-update

在弹出的窗口中询问是否将目录转化为英文路径,同意并关闭.
在终端中输入命令:
export LANG=zh_CN

2. 扩展
default:
    Applications menu
    Place status indicator
    User themes
    Background logo
    Launch new instance
    User themes
    Window list

install:
    simple net speed

remove:
    NetSpeed (incompatible 3.20)
    Taskbar

3. Gnome3 debugger and inspector tool: 
   https://wiki.gnome.org/Projects/GnomeShell/LookingGlass
   
   查找某个扩展安装的位置：
       Alt+f2，输入 lg，点击 extensions，点击该扩展下面的查看源
       Alt+
